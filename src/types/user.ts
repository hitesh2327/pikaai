export interface User {
    id: string;
    username: string;
    email: string;
    fullName: string;
    avatarUrl?: string;
    timezone?: string;
    phoneNumber?: string;
    accountId?: string;
}
